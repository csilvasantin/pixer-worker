{
  "name": "pixer-eleven",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker — bridge Pixer.ai (capa 1) ↔ AdmiraXP / XpaceOS (capa 2). ElevenLabs / xAI / GCP / Signage feed.",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.90.0"
  }
}
